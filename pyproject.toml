[project]
name = "chicago-traffic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = ["src"]
